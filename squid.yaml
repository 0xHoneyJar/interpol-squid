manifestVersion: subsquid.io/v0.1
name: interpol-mainnet-v1
version: 1
description: "Interpol Mainnet v1 Squid"
build:
deploy:
  env:
    PORTAL_URL: ${{ addons.portal.berachain_bartio.http }}
  addons:
    postgres:
    # rpc:
    #   - eth.http
  processor:
    cmd: ["sqd", "process:prod"]
  api:
    cmd: ["sqd", "serve:prod"]
scale:
  dedicated: true
  addons:
    postgres:
      storage: 50G
